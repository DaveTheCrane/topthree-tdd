package com.topthree;

import org.junit.jupiter.api.Test;

class SmokeTest {

    @Test
    void buildToolchainIsConfiguredCorrectly() {
        // This test exists solely to verify that the Maven build toolchain is configured
        // and working correctly. If this test runs and passes, we can proceed with TDD.
    }
}
